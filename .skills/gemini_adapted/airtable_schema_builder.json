{
  "name": "airtable_schema_builder",
  "description": "Creates and modifies Airtable bases, tables, and fields via the Airtable Meta API.",
  "parameters": {
    "type": "object",
    "properties": {
      "base_id": {
        "type": "string",
        "description": "The target Airtable Base ID"
      },
      "schema_payload": {
        "type": "string",
        "description": "JSON representation of the table and field definitions to construct"
      }
    },
    "required": ["base_id", "schema_payload"]
  }
}
