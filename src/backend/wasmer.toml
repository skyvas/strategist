[package]
name = "restaurant-waitlist-api"
version = "1.0.0"
description = "15-Table Restaurant Waitlist and Real-Time Table Management API"
license = "MIT"

[dependencies]

[[command]]
name = "server"
module = "python"
runner = "wasi"

[fs]
"/app" = "."

[env]
PORT = "8080"
HOST_PIN = "1515"
DATABASE_URL = "postgres://postgres:postgres@localhost:5432/restaurant"
